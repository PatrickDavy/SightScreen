Square icon-only button for toolbar-ish actions (settings, close, share); always pass `label`.

```jsx
<IconButton name="settings" label="Settings" />
<IconButton name="x" label="Close" size="sm" />
```

Variants ghost (default) / primary / secondary; sizes sm 28 / md 36 / lg 44.
