Flat white container with hairline border and 8px radius — the default surface on the chalk background.

```jsx
<Card title="This week" action={<Badge tone="good">Within limit</Badge>}>…</Card>
```

`raised` adds the only card shadow allowed. Ink-inverse cards are NOT made with Card — that treatment belongs to `CueCard` alone.
