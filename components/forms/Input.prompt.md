Text field; labels are short caps ("HEIGHT"), units go in `suffix`, errors are honest and say what to do.

```jsx
<Input label="Height" suffix="cm" inputMode="numeric" placeholder="178" />
<Input label="Name" error="Needs at least two characters." />
```
