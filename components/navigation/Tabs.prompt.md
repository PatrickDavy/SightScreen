Underline tabs for sectioning one screen (Overview · Balls · Trends); 2–4 items, short labels.

```jsx
<Tabs items={[{id:'overview',label:'Overview'},{id:'balls',label:'Balls'}]} value={tab} onChange={setTab} />
```
