Segmented toggle for 2–3 one-word options — units, view modes. The product's km/h · mph switch.

```jsx
<SegmentedControl options={["km/h","mph"]} value={unit} onChange={setUnit} />
```

Active segment fills ink. More than 3 options → Select.
